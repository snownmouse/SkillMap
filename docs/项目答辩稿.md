# SkillMap（技能地图）—— 项目答辩稿

## 目录

1. 项目背景与选题依据
2. 行业痛点与需求分析
3. 解决方案概述
4. 核心功能展示
5. 理论创新（中国特色生涯规划体系）
6. 技术架构与创新点
7. 已解决问题与当前成果
8. 待解决问题与改进计划
9. 未来规划与演进路线
10. 总结

---

## 一、项目背景与选题依据

### 1.1 时代背景

当前我国正处于从"人口红利"向"人才红利"转型的关键阶段。教育部明确提出要"构建中国特色生涯教育自主知识体系"，强调将**立德树人、为党育人、为国育才**作为教育的根本任务。

与此同时，AI 大模型技术的爆发为个性化教育带来了前所未有的可能——传统"千人一面"的课程表可以被"千人千面"的智能学习路径所取代。

### 1.2 选题依据

本项目的选题基于三个核心判断：

1. **需求真实**：大学生和职场新人普遍面临"不知道该学什么、先学什么、后学什么"的迷茫
2. **技术可行**：大语言模型能够基于用户输入生成结构化、个性化的学习方案
3. **理论创新空间**：现有生涯教育工具多为西方理论主导，缺乏中国特色的理论融合

### 1.3 产品定位

**SkillMap 是一款对话驱动的 AI 技能探索与生涯规划平台**——以"技能树"为核心隐喻，帮助用户从"认知自我"到"规划未来"。

---

## 二、行业痛点与需求分析

### 2.1 传统学习规划的四大痛点

| 痛点 | 具体表现 | 用户损失 |
|------|---------|---------|
| **路径不清晰** | 课程目录≠成长路径，用户不知道技能之间的先后依赖关系 | 学习效率低，容易走弯路 |
| **方案不个性** | 所有人用同一套课程大纲，不区分专业背景、目标职业和当前水平 | 基础好的人浪费时间，基础差的跟不上 |
| **反馈不及时** | 学完了不知道自己掌握到什么程度，缺乏结构化复盘机制 | 虚假的"学完了"，真实的一知半解 |
| **规划不落地** | 职业规划停留在"性格测试"层面，缺少可执行的具体步骤 | 知道方向，不知道怎么走 |

### 2.2 现有产品的局限性

- **职业测评类产品**（如 MBTI、霍兰德测试）：只有匹配结果，没有学习路径
- **在线课程平台**（如 Coursera、B站）：课程列表是线性的，不展示技能间的拓扑关系
- **AI 对话工具**（如 ChatGPT）：回答泛化，缺乏围绕技能体系的深度互动

**SkillMap 的核心突破**：将"职业测评 → 技能树生成 → 节点对话复盘 → 进度追踪"串成闭环，填补了从"知道方向"到"走到目标"之间的空白。

---

## 三、解决方案概述

### 3.1 核心思路

```
用户输入（专业 + 目标职业 + 当前水平）
     ↓
AI 智能生成个性化技能树（25-40 个节点，含依赖关系）
     ↓
可视化交互图谱（Cytoscape.js 渲染，节点状态一目了然）
     ↓
围绕每个技能节点的多轮 AI 对话复盘
     ↓
进度追踪 + 时间线回看 + 职业路径规划
```

### 3.2 系统架构（一张图）

```
┌──────────────────────────────────────────────────────────┐
│                     用户浏览器                            │
│  React 19 · TypeScript · Cytoscape · Motion · Tailwind   │
│  WebSocket 实时连接  │  HTTP REST API                     │
└───────────────────────┬──────────────────────────────────┘
                        │
┌───────────────────────▼──────────────────────────────────┐
│                    Express API 服务                       │
│  Controller → Service → Repository → Database             │
│  Middleware: 安全校验 · 速率限制 · 请求追踪 · 错误处理     │
└───────────────────────┬──────────────────────────────────┘
                        │
        ┌───────────────┼───────────────┐
        ▼               ▼               ▼
┌───────────┐  ┌───────────────┐  ┌──────────────┐
│ PostgreSQL│  │  Redis 队列    │  │  LLM 适配层   │
│  / SQLite │  │  PubSub 推送   │  │ 多模型熔断切换│
└───────────┘  └───────┬───────┘  └──────┬───────┘
                        │                  │
                ┌───────▼───────┐  ┌───────▼───────┐
                │  Worker 进程   │  │ Ark · DeepSeek│
                │  异步任务执行   │  │ Gemini · Qwen │
                └───────────────┘  └───────────────┘
```

---

## 四、核心功能展示

### 4.1 智能技能树生成

**输入**：用户填写专业、目标职业、当前水平、每周学习时长、已有技能、备注

**输出**：一棵包含 25-40 个节点的结构化技能树，每个节点包含：
- 节点名称、描述、为什么重要
- 难度等级（初级/中级/高级）
- 认知层级（Bloom's Taxonomy 六层）
- 学习目标、交付物、学习资源
- 推荐工具、实践技巧、常见陷阱
- 掌握标准（最低合格 / 熟练 / 精通）
- 预估学习时长

**关键技术**：提示词工程融合了 Bloom's Taxonomy、认知负荷理论、建构主义、最近发展区等教育理论

### 4.2 可视化技能图谱

- 使用 **Cytoscape.js** 渲染为交互式网络图
- 节点颜色区分技能类别（核心 / 专业 / 通用）
- 节点样式区分学习状态（未解锁 / 可学习 / 学习中 / 已完成）
- 方向箭头展示技能前置依赖关系
- 支持缩放、拖拽、点击查看详情
- Motion 动画库实现流畅过渡效果

### 4.3 节点对话复盘

- 每个技能节点拥有独立的 AI 对话空间
- 对话上下文锚定在当前技能领域，回答更聚焦
- 对话心得可回写至节点进度，形成"学习→复盘→进步"的正循环
- 历史对话持久化存储，可随时回溯

### 4.4 职业路径规划

基于 8 个维度（霍兰德匹配、职业锚匹配、家国情怀、义利兼顾、明德弘道、实践志向、国家战略需求、社会贡献预期）的加权评分，为用户推荐 7 条差异化成长路径：

- 技术深耕路线 → 国家战略：科技自立自强
- 管理发展路线 → 高素质管理人才培养
- 复合发展路线 → 跨界复合型人才
- 基层锻炼路线 → 乡村振兴
- 国家战略路线 → 重点领域突破
- 创新创业路线 → 大众创业
- 稳定发展路线 → 公共服务

### 4.5 学习时间线

将用户的技能解锁、节点完成、对话复盘等事件按时间轴展示，形成可视化的成长轨迹。

---

## 五、理论创新——中国特色生涯规划体系

### 5.1 为什么要做理论创新

现有生涯教育工具几乎全部基于西方理论（Holland Code、Career Anchors 等），缺乏与中国教育理念的结合。在"立德树人"的根本任务下，生涯规划不能只关注个人兴趣匹配，需要融入**社会责任感与国家战略视野**。

### 5.2 "个人—社会—国家"三位一体框架

```
┌─────────────────────────────────────────────┐
│           中国特色生涯规划体系               │
├─────────────────────────────────────────────┤
│  ┌───────────┐  ┌───────────┐  ┌─────────┐ │
│  │  个人维度  │  │  社会维度  │  │ 国家维度 │ │
│  │ (西方理论) │  │ (中西融合) │  │(中国特色)│ │
│  └───────────┘  └───────────┘  └─────────┘ │
│  · 兴趣匹配    · 社会责任     · 国家战略   │
│  · 能力评估    · 职业道德     · 区域协调   │
│  · 价值取向    · 义利兼顾     · 基层锻炼   │
│  · 目标设定    · 家国情怀     · 民族复兴   │
└─────────────────────────────────────────────┘
```

### 5.3 中西理论融合对照

| 维度类型 | 理论来源 | 评估内容 |
|---------|---------|---------|
| 霍兰德匹配度 | Holland Code（西方） | 职业兴趣匹配 |
| 职业锚匹配度 | Career Anchors（西方） | 价值观契合度 |
| 家国情怀 | 中华优秀传统文化 | 服务国家意愿 |
| 义利兼顾 | 中华优秀传统文化 | 利益与责任平衡 |
| 明德弘道 | 中华优秀传统文化 | 职业道德修养 |
| 实践志向 | 马克思主义 | 基层实践意愿 |
| 国家战略需求 | 社会主义教育理论 | 战略契合度 |
| 社会贡献预期 | 马克思主义人的全面发展 | 社会贡献价值 |

### 5.4 理论创新核心原则

1. **中体西用**：以中国特色理论为主体框架，融合西方科学方法
2. **知行合一**：理论指导与实践锻炼并重
3. **家国同构**：个人发展与国家命运紧密相连
4. **双层分离**：数据处理理论（精确计算）≠ 用户展示理论（通俗解释）

---

## 六、技术架构与创新点

### 6.1 技术栈一览

| 层级 | 技术选型 |
|------|---------|
| 前端 | React 19 · TypeScript · Vite · Tailwind CSS v4 · Cytoscape.js · Motion |
| 后端 | Node.js · Express · TypeScript |
| 数据库 | SQLite（开发）· PostgreSQL（生产）· 版本化迁移 |
| 队列/缓存 | Redis Stream · Redis PubSub |
| AI 模型 | Ark · DeepSeek · SiliconFlow · Gemini · Qwen · 自定义 OpenAI 兼容 |
| 部署 | Docker Compose · PM2 · GitHub Actions CI/CD |

### 6.2 八大技术创新点

#### 创新点 1：多模型熔断切换机制

实现了统一的 LLM 适配层（Provider Pattern），支持 6 种以上 AI 模型的一键切换与自动容灾：

- 主提供商不可用时，自动切换至备用提供商
- 内置调用日志与 Token 用量统计
- 支持 Dummy Provider 用于离线开发和测试

#### 创新点 2：异步任务队列 + WebSocket 实时推送

技能树生成属于长耗时 LLM 调用，系统采用异步任务架构：

```
用户请求 → 创建任务（落库）
       ↓
  Redis Stream 入队
       ↓
  Worker 独立进程消费
       ↓
  执行完成 → DB + WebSocket 推送
       ↓
  前端实时订阅/轮询获取结果
```

关键机制：
- 任务状态全生命周期追踪（pending → processing → completed → failed）
- Redis PubSub 跨实例广播，多实例部署无缝协作
- 数据库租约防重复执行，服务重启自动恢复
- 指数退避自动重试 + 定时调度器

#### 创新点 3：双数据库自适应模式

| 模式 | 数据库 | 触发条件 |
|------|--------|---------|
| 开发模式 | SQLite | 默认，`DB_PATH` 配置，零依赖 |
| 生产模式 | PostgreSQL | `NODE_ENV=staging|production` 强制启用 |

- 4 个版本化迁移脚本，支持升级/回滚
- 服务启动自动执行未应用迁移
- 环境自适应，无需手动切换

#### 创新点 4：多层安全防护

| 防护层 | 机制 |
|--------|------|
| 身份认证 | Bearer Token + Refresh Token 双令牌 + HttpOnly Cookie 访客会话 |
| 密码安全 | bcrypt 哈希 + 旧 SHA-256 渐进式迁移 |
| Token 安全 | `h1:sha256(rawToken)` 哈希存储，防库泄露接管 |
| API 限流 | 15 分钟窗口，支持 Redis store / 内存降级 |
| 输入安全 | XSS 清洗 + Prompt Injection 防护 |
| 响应安全 | CSP / X-Frame-Options / X-Content-Type-Options 安全头 |
| WS 安全 | Origin 白名单 + Task 归属校验 + 连接/消息限流 |
| 请求追踪 | 全局 Trace ID，全链路可审计 |

#### 创新点 5：JSON 增量解析 + 自动修复

大语言模型的 JSON 输出天然存在格式不稳定、不完整、字段遗漏的问题。系统自研了两大工具：

- **IncrementalJsonParser**：支持流式增量解析，边接收边渲染，用户体感延迟大幅降低
- **JsonRepairUtils**：自动修复缺失引号、多余逗号、截断、未闭合括号等常见 JSON 格式错误

#### 创新点 6：前后端一体化 + 进程分离

- **开发阶段**：单进程 Express + Vite 中间件，HMR 热更新秒级响应
- **生产阶段**：API 进程 + Worker 进程分离，互不阻塞

```yaml
# docker-compose.yml
services:
  app:      # API + HTTP + WS
  worker:   # Redis Stream consumer (独立扩缩)
  postgres: # 数据库
  redis:    # 队列 + PubSub + 限流 store
```

#### 创新点 7：语音交互能力

内置 `useVoiceInput`（STT）和 `useVoiceOutput`（TTS）两个自定义 Hook，拓展无障碍使用场景。

#### 创新点 8：提示词工程深度融合教育理论

提示词模板整合了 Bloom's Taxonomy、建构主义、联通主义、认知负荷理论、刻意练习、OKR 目标管理等 7+ 教育理论，确保 AI 输出的技能树兼具科学性与实用性。

---

## 七、已解决问题与当前成果

### 7.1 已解决的 P0 级（阻断型）问题

| 问题 | 解决方案 | 状态 |
|------|---------|------|
| 访客身份可被冒用 | HttpOnly Cookie `sm_guest` + DB `guest_sessions` 映射 | ✅ 已完成 |
| CORS 默认 `*` 且 credentials | 生产强制白名单，`*` 自动禁用 credentials | ✅ 已完成 |
| WS token 走 URL 泄露风险 | Cookie 会话 + Origin 白名单 + Task 归属校验 | ✅ 已完成 |
| 生产日志打印明文输入 | 移除所有生成链路 `console.log(inputs)` | ✅ 已完成 |
| PostgreSQL UUID 方言不一致 | 统一应用层 UUID 生成 | ✅ 已完成 |

### 7.2 已解决的 P1 级（上线前）问题

| 问题 | 解决方案 | 状态 |
|------|---------|------|
| 密码 SHA-256 无盐存储 | 升级为 bcrypt，旧用户渐进迁移 | ✅ 已完成 |
| Token 明文存储 | Token 存储改为 `h1:sha256` 哈希 | ✅ 已完成 |
| HTTP 限流不支持多实例 | 支持 Redis store，未配置自动降级内存 | ✅ 已完成 |
| WS 无限流 | 连接数 + 消息频率基础限流 | ✅ 已完成 |
| 任务状态不落库 | 任务状态落库（含 user_id/inputs），重启可恢复 | ✅ 已完成 |
| 多实例 WS 不互通 | Redis PubSub 跨实例转发 task_update | ✅ 已完成 |
| 无队列机制 | Redis Stream + consumer group worker + DB 租约 | ✅ 已完成 |
| 无自动重试 | 指数退避自动重试 + 定时调度器 + `retry` API | ✅ 已完成 |

### 7.3 当前成果

| 维度 | 成果 |
|------|------|
| **代码规模** | 200+ 源文件，TypeScript 全栈 |
| **API 端点** | 30+ REST API + WebSocket |
| **数据库表** | 10+ 核心表，4 次版本化迁移 |
| **LLM 提供商** | 6 种模型，支持熔断切换 |
| **安全防护** | 6 层安全机制落地 |
| **部署方式** | Docker Compose 一键拉起 + PM2 集群 + CI/CD |
| **测试覆盖** | Prompt 单元测试 20+ 用例，含边界/异常全覆盖 |
| **文档体系** | architecture / setup / api / operations 四大类 15+ 篇文档 |

---

## 八、待解决问题与改进计划

### 8.1 安全问题（P1 待完善）

| 编号 | 问题 | 优先级 | 计划 |
|------|------|--------|------|
| P1-1 | 缺登录失败计数与冻结策略（防暴力破解） | 高 | 2 周内完成 |
| P1-2 | 限流未迁移到网关层（生产应统一策略） | 高 | 部署时配置 Nginx/API Gateway |
| P1-4 | 权限模型存在 legacy default-user 兼容逻辑 | 高 | 一次性数据迁移 + 移除兼容代码 |
| P1-5 | 导入/更新接口缺少 Schema 级校验（DoS 防护） | 中 | 引入 JSON Schema 校验 |

### 8.2 工程质量（P2）

| 编号 | 问题 | 优先级 | 计划 |
|------|------|--------|------|
| P2-1 | Prompt → 后端 → 前端类型一致性（避免数据漂移） | 中 | 版本化 schema + 契约测试 |
| P2-2 | 结构化日志不全 + metrics 无鉴权 | 中 | 统一日志格式 + 内网暴露鉴权 |
| P2-3 | auth 逻辑/DB 方言重复散落 | 低 | 收敛到统一模块 |

### 8.3 产品功能缺失

| 功能 | 理论支撑 | 价值 | 计划 |
|------|---------|------|------|
| **学习效果客观评估** | Mastery Learning + CMM | 用户无法确认真实掌握度 | 2-4 周 |
| **复习提醒系统** | Spaced Repetition + Forgetting Curve | 解决"学完就忘" | 2-4 周 |
| **自适应难度调节** | Zone of Proximal Development | 提升学习匹配度 | 1-2 月 |
| **学习动机维持** | Self-Determination Theory + Flow | 降低中途放弃率 | 1-2 月 |
| **用户学习洞察** | Learning Analytics | 提供个性化建议 | 2-3 月 |
| **测试体系完善** | Test Pyramid（单元/集成/E2E） | 保障代码质量 | 持续 |

---

## 九、未来规划与演进路线

### 9.1 短期目标（1 个月内）

- 补齐 P1 级安全漏洞（防暴力破解 + Schema 校验 + 权限收敛）
- 上线复习提醒系统（Spaced Repetition）
- 掌握标准定义（Mastery Learning Criteria）
- 生产环境网关层限流配置

### 9.2 中期目标（1-3 个月）

- 自适应难度调节引擎（ZPD + Flow Theory）
- 用户学习洞察看板（Learning Analytics Dashboard）
- 完善单元测试 + 集成测试体系
- 社交功能（学习小组 + 技能分享）

### 9.3 长期愿景（3-12 个月）

- **公网多用户上线**：完成全部安全与稳定性基线
- **企业版**：支持多租户、自定义技能模板、团队学习路径
- **移动端**：React Native 跨平台客户端
- **AI 教育研究平台**：为学术研究提供匿名化学习行为数据集
- **学习路径优化引擎**：基于用户群体数据持续优化推荐算法

### 9.4 部署路径

```
内网多用户（当前）  →  补齐 P0/P1 基线  →  公网多用户 Beta  →  正式发布
```

---

## 十、总结

### 10.1 一句话总结

**SkillMap 是一个以"技能树"为核心结构、以生涯教育理论为科学依据、以 AI 大模型为技术引擎的个人成长导航系统。**

### 10.2 核心竞争力

| 维度 | 核心价值 |
|------|---------|
| **产品创新** | 技能树可视化 + 节点对话复盘，打造"结构化 AI 学习"新范式 |
| **理论创新** | 中西融合的中国特色生涯规划体系——"个人—社会—国家"三位一体 |
| **技术创新** | 多模型熔断 + 异步任务队列 + WebSocket 推送 + JSON 修复引擎 |
| **安全成熟** | 6 层防护：认证/加密/限流/清洗/追踪/WS 隔离 |
| **工程可靠** | 双数据库 + 版本化迁移 + Docker 化 + CI/CD + Worker 分离 |
| **场景广泛** | 覆盖高校生涯教育、职场技能提升、职业转型、企业培养四大场景 |

### 10.3 核心价值主张

> **不是又一个"AI 对话机器人"，也不是又一个"职业测评工具"——**
> **它连接了"测评 → 规划 → 学习 → 复盘"的完整闭环。**
>
> 让每一个人，都能看清自己的技能版图，找到最优的成长路径。

---

*答辩稿版本：v1.0*
*生成日期：2026-05-12*