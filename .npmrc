# 跨平台 optionalDependencies（如 @tailwindcss/oxide-linux-x64-gnu / -win32-x64-msvc）
# 默认就会被安装；这里仅作说明，避免部署时误加 --omit=optional 导致原生模块缺失。
#
# 镜像源示例（默认走 npm 官方源；国内服务器可按需取消注释切换）：
# registry=https://mirrors.cloud.tencent.com/npm/
# 或：registry=https://registry.npmmirror.com/
