# SkillMap 设计规范文档

## 1. 视觉风格
项目采用 **深色模式 (Dark Mode)** 为主基调，旨在营造专注、科技感的学习氛围。

### 色彩规范 (Tailwind 4 配置)
- **背景**: `#0a0a0f` (深邃黑)
- **卡片/面板**: `#141420` (深灰蓝)
- **核心技能 (Core)**: `#4A90D9` (科技蓝)
- **专精技能 (Specialization)**: `#E67E22` (活力橙)
- **通用技能 (General)**: `#9B59B6` (优雅紫)

## 2. 交互规范

### 技能树画布 (Cytoscape.js)
- **布局**: 采用 `breadthfirst` (广度优先) 布局，清晰展示技能依赖。
- **状态反馈**:
  - `Locked`: 灰色边框，低透明度。
  - `Available`: 亮色边框，表示可开始。
  - `In Progress`: 呼吸灯动画效果。
  - `Completed`: 绿色勾选标记或高亮边框。

### 对话体验
- **侧边栏**: 点击节点后从右侧滑出详情与对话面板。
- **打字机效果**: AI 回复应有平滑的出现效果。
- **进度同步**: 对话中提到的进度更新应实时反映在画布节点上。

## 3. 数据结构规范

### 技能节点 (SkillNode)
每个节点必须包含：
- `id`: 唯一标识。
- `name`: 中文名称。
- `status`: 状态机管理（locked -> available -> in_progress -> completed）。
- `progress`: 0-100 的数值。

## 4. 动画规范
- **页面切换**: 使用 `motion` 实现淡入淡出。
- **生成过程**: 专门的 `GenerateAnimation` 组件，模拟“技能生长”的视觉过程。
